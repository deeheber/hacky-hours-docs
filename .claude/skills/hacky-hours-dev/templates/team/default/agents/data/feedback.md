# Durable feedback — data

No feedback yet. As the conductor gives this agent corrections or confirmations during sessions, durable notes accumulate here:
- "be terser when flagging at Tier 1"
- "always include enforcement mechanism, never just a rule"
- "stop using the word X — it's confusing"

Feedback captured during a session lands in `~/.hacky-hours/sessions/<id>/pending/<agent>.md` first; `/hacky-hours team update` promotes accepted items here.
