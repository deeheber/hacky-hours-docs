# Track record — ai-ml

No work history yet. This file accumulates a resume-style record as this agent participates in projects:
- per-project entries (date, project, what they did)
- patterns observed (what they tend to flag, what they tend to defer on)
- key facts about how the conductor likes them to work

Compaction protocol: when this file exceeds ~500 lines or ~10k tokens, the framework summarizes older entries into a "Key facts and patterns" section at the top and archives raw entries to `history-archive/<date>.md`. Git history preserves the full record either way.
